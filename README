Contains config files for all the programs that I use.

Locations and names for these files:
   bashrc         ${HOME}/.bashrc
   emacs          ${HOME}/.emacs
   emacs_mac      ${HOME}/.emacs
   psqlrc         ${HOME}/.psqlrc
   Rprofile       ${HOME}/.Rprofile
   ssh_config     ${HOME}/.ssh/config   # Alson needs: chmod og-rwx ${HOME}/.ssh
   tmux.conf      ${HOME}/.tmux.conf
   vimrc          ${HOME}/.vimrc
   zshrc          ${HOME}/.zshrc

